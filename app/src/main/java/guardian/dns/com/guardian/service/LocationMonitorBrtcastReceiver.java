package guardian.dns.com.guardian.service;

import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;

/**
 * Created by dinusha on 5/18/16.
 */
public class LocationMonitorBrtcastReceiver extends BroadcastReceiver{

    @Override
    public void onReceive(Context context, Intent intent) {
      //  Intent lMSvsIntent = new Intent(context,LocationMonitorService.class);
       // context.startService(lMSvsIntent);
    }
}
